import axios from 'axios';


jest.mock('axios');

describe('movie cast reducer test', () => {
    let response;
    beforeEach(() => {
        response = {
            data: [

            ]
        };
    });
});